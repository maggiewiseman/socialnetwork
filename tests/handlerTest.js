
var expect = require('chai').expect;
var assert = require('chai').assert;


describe('handle searchName', function() {
    it('should return user_id, first_name and last_name for a string that matches the first_name or user_name in the database');
    
    it('should return return an empty array if the string does not exist in the database');

})
